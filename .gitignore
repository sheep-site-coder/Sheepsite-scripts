.DS_Store
credentials/*.json
config/*.json

# Chatbot: distilled governing docs (building-specific, generated during setup)
faqs/*_rules.md

# Chatbot: live credit data (server-managed, upload manually)
faqs/woolsy_credits.json

# Local working files — PDFs, screenshots, etc.
Sharefolder/

# Local deployment staging — never commit
_deploy/

# Server-side Drive listing cache — runtime files, not in git
cache/
